import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Button("OK") {
                // TODO: アクションを追加
            }
            .buttonStyle(.borderedProminent)
            .controlSize(.large)
        }
    }
}

#Preview {
    ContentView()
}
